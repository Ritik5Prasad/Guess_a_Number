     // <ScrollView style={styles.scroll}>
                   //          <Text style={DefaultStyle.opponent}>Past Guesses</Text>
                   //          {pastGuesses.map
                   //            ((guess,index) => renderListItem(guess,index+1)
                   //  )}              
                   // </ScrollView>

                   //USING FLAT LIST 
// key ={value}
and toSring remove